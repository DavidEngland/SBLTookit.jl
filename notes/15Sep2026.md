An advanced graduate-level curriculum bridges discrete control theory, atmospheric thermodynamics, and numerical weather prediction (NWP) by using the 4-link discrete feedback loop as its foundational pillar:

$$\mathcal{G}_{\text{loop}} = F'(Ri_g^*) = \underbrace{\left( \frac{\partial Ri_g}{\partial \theta_z} \right)}_{\text{Diagnostic Sensor}} \cdot \underbrace{\left( \frac{\partial \theta_z}{\partial H} \right)}_{\text{Plant Impedance}} \cdot \underbrace{\left( \frac{\partial H}{\partial K_h} \right)}_{\text{Flux Actuator}} \cdot \underbrace{\left( \frac{\partial K_h}{\partial Ri_g} \right)}_{\text{Closure Slope}}$$

This four-link product is a selected path gain. It is the complete scalar Jacobian only when the computational graph factors through these four variables. Because $H=-\rho c_p K_h\theta_z$ depends on both $K_h$ and $\theta_z$, the full linearization also contains

$$\delta H=-\rho c_p\theta_z\,\delta K_h-\rho c_pK_h\,\delta\theta_z,$$

as well as any neighboring-level couplings in a multilayer column. The complete SCM stability object is therefore the Jacobian $J=\partial\mathbf{x}^{n+1}/\partial\mathbf{x}^n$.

---

## Module 1: Control Theory of Atmospheric Boundary Layers

* **Lecture 1.1: The Discrete Gain Chain ($\mathcal{G}$)**
* Derivation of the discrete iteration map $x_{n+1} = f(x_n)$ for atmospheric column models.
* Local scalar fixed-point stability ($\vert{}\mathcal{G}\vert{} < 1$) versus BIBO stability of a discrete input-output system.

* **Lecture 1.2: State-Space Representation of Planetary Boundary Layer (PBL) Columns**
* Expressing coupled potential temperature ($\theta$) and momentum ($u, v$) profiles as continuous-time plant matrices.
* Linearization around nocturnal equilibrium states.

---

## Module 2: Thermal Plant Impedance & Surface Energy Budget (SEB)

* **Lecture 2.1: Surface Radiative Cooling & Energy Coupling**
* The surface energy balance equation: $R_n + H_0 + L E + G = 0$.
* Deriving a reduced-order process approximation $P_H \equiv \frac{\partial \theta_z}{\partial H} \sim \frac{\Delta t}{\rho c_p(\Delta z)^2}$; the exact sign and coefficient follow from the finite-volume stencil and boundary condition.

* **Lecture 2.2: Thermal Decoupling & Cold-Pool Divergence**
* Mechanics of heat flux shutdown ($H_0 \to 0$): How skin temperature ($T_s$) drops trigger rapid lapse-rate steepening ($\theta_z$).
* Case studies in single-column models (SCMs): Spurious cold biases in GABLS3 and Arctic inversions.

---

## Module 3: Sensor Amplification & Actuator Switching Physics

* **Lecture 3.1: The Diagnostic Sensor Gain ($\partial Ri_g / \partial \theta_z$)**
* Mathematical expansion of $\frac{\partial Ri_g}{\partial \theta_z} = \frac{g}{\theta_0 S^2}$.
* Singularity at the Low-Level Jet (LLJ) nose: Weak vertical shear ($S \to 0$) gives diagnostic gain $\mathcal{O}(S^{-2})$ and variance amplification $\mathcal{O}(S^{-6})$ when shear-error variance is approximately constant.

* **Lecture 3.2: Turbulence Closure Actuators ($\partial K_h / \partial Ri_g$)**
* Monin–Obukhov Similarity Theory (MOST) short-tail closures vs. empirical long-tail functions.
* Closure-specific inverse-map singularities, including cases where $\mathcal{K}_h' \to -\infty$ near $Ri_c \approx 0.20$.

---

## Module 4: Spatial Geometry, Grid Resolution & Profile Curvature

* **Lecture 4.1: The Role of Vertical Profile Curvature ($\partial^2 Ri_g / \partial z^2$)**
* Proving exact discrete evaluation under linear profile conditions ($\frac{\partial^2 Ri_g}{\partial z^2} = 0$).
* Grid-scale error generation: How under-resolved profile knees interact with grid cell thickness ($\Delta z$).

* **Lecture 4.2: Macro vs. Micro Grid Corrections**
* Downstream scaling factors $f_c(Ri, \Delta z)$ across coarse climate grids (McNider & Pour-Biazar framework).
* Upstream scalar mapping vs. downstream empirical retuning.

---

## Module 5: Regularization & Implicit Numerical Solvers

| Lecture | Mathematical Focus | Physical / Algorithmic Target |
| --- | --- | --- |
| **5.1: $C^\infty$ Hyperbolic Operators** | Upstream C-Z0HR mapping $\widetilde{Ri}_g$ & soft-plus norms $\Phi_\epsilon(x)$ | Eliminating non-differentiable $C^0$ kinks at $Ri_c$. |
| **5.2: Slope Attenuation Derivation** | Derivation of $\mathcal{D}_\alpha^{(R)}(Ri_c)$, including finite $C$ and $\epsilon_c$ limits | Bounding local feedback sensitivity near threshold. |
| **5.3: Implicit Jacobian Stability** | Full SCM Jacobian, including $\frac{\partial \widetilde{Ri}_{g,k}}{\partial Ri_{g,k\pm 1}}$ | Assessing $\rho(J_{\rm reg})<1$ and preventing Newton--Raphson stalls in NWP cores. |

---

## Module 6: Benchmark Audits & Advanced Applications

* **Lecture 6.1: Field Campaign Audits**
* Diagnostic evaluations across CASES-99, GABLS3, and SHEBA data profiles.
* Supercritical residual tails ($K_m > 0$) maintaining operator hyperbolicity.

* **Lecture 6.2: 4D-Var Data Assimilation & Tangent Linear Models**
* Gradient propagation through regularized closures in operational weather forecasting.

---

Would you like to expand the detailed mathematical derivations or reading lists for any specific lecture module?

Evaluating the continuous components of this four-link chain rule demonstrates how numerical resolution and physical closures interact to trigger numerical instability:

$$\mathcal{G}_{\text{physical}} = \underbrace{\left( \frac{g}{\theta_0 S^2} \right)}_{\text{Diagnostic Sensor}} \cdot \underbrace{\left( \frac{\partial \theta_z}{\partial H} \right)}_{\text{Plant Impedance}} \cdot \underbrace{\left( -\rho c_p \theta_z \right)}_{\text{Flux Actuator}} \cdot \underbrace{\left( l_h^2 S \, f_h'(Ri_g) \right)}_{\text{Closure Slope}}$$

**Component Evaluation Across the Feedback Loop**

* **Diagnostic Sensor Gain ($\partial Ri_g / \partial \theta_z = \frac{g}{\theta_0 S^2}$):** Scales as $\mathcal{O}(S^{-2})$. In weak-shear layers ($S \to 0$), such as the nose of a nocturnal Low-Level Jet, this term acts as an unbounded amplifier, magnifying tiny fluctuations in lapse rate ($\delta \theta_z$).
* **Process Transfer Impedance:** A layer tendency gives $\theta_k^{n+1}-\theta_k^n = -\frac{\Delta t}{\rho c_p\Delta z}(H_{k+1/2}-H_{k-1/2})+\cdots$. The derivative of a temperature gradient with respect to a flux therefore scales like $P_H\sim\frac{\Delta t}{\rho c_p(\Delta z)^2}$ for some stencils; its exact sign and coefficient are discretization-dependent.
* **Flux Actuator Sensitivity ($\partial H / \partial K_h = -\rho c_p \theta_z$):** Dictates the linear coupling between eddy diffusivity $K_h$ and surface heat flux $H_0$.
* **Closure Derivative ($\partial K_h / \partial Ri_g = l_h^2 S \, f_h'(Ri_g)$):** For closures obtained by inverting a similarity mapping with a finite asymptote $Ri_c$, the inverse derivative can diverge as $Ri\to Ri_c^-$. This is closure-specific, not a universal property of MOST.

**Regularized Fixed-Point Stability**

When C-Z0HR is applied upstream, the chain rule expands into a five-term product by inserting the partial slope attenuator $\mathcal{D}_\alpha^{(R)}(Ri_g) \equiv \left.\frac{\partial \widetilde{Ri}_g}{\partial Ri_g}\right\vert{}_{C_\eta}$:

$$\mathcal{G}_{\text{reg}} = \mathcal{G}_{\text{physical}} \cdot \mathcal{D}_\alpha^{(R)}(Ri_g)$$

At exact threshold crossing ($Ri_g = Ri_c$), the attenuation factor limits local sensitivity to $\mathcal{D}_\alpha^{(R)}(Ri_c) \approx \frac{1}{1 + \alpha}$. For standard damping control ($\alpha = 2.0$), $\mathcal{D}_2^{(R)} = 1/3$, scaling down the discrete derivative so that:

$$\left\vert{} \mathcal{G}_{\text{physical}}(Ri_c) \right\vert{} < 3 \implies \left\vert{} \frac{d(Ri_g^{\text{new}})}{d(Ri_g^{\text{old}})} \right\vert{} < 1$$

This is a local scalar fixed-point condition, not a BIBO criterion. For a multidimensional SCM, the corresponding local condition is $\rho(J_{\rm reg})<1$; BIBO stability should be treated separately for the relevant input-output system.

---

The **Control Theory of Atmospheric Boundary Layers (ABLs)** provides a unified mathematical framework that bridges discrete numerical stability, control systems engineering, and planetary boundary layer (PBL) thermodynamics. By framing a 1D single-column model (SCM) coupled with a land-surface energy budget (SEB) as a **closed-loop feedback system**, control theory explains why unregularized first-order closures undergo unphysical numerical divergence—and how to stabilize them.

---

### 1. The Discrete Loop Gain Chain (\(\mathcal{G}\))

In a discretized atmospheric model, the evolution of local stability across time steps \(n \to n+1\) can be modeled as a discrete 1D update map \(x_{n+1} = f(x_n)\). The **total loop gain (\(\mathcal{G}\))** is defined as the total derivative of the Richardson number with respect to itself across one full numerical feedback iteration:

\[\mathcal{G} \equiv \frac{d(Ri_g^{\text{new}})}{d(Ri_g^{\text{old}})} = \underbrace{\left( \frac{\partial Ri_g}{\partial \theta_z} \right)}_{\text{1. Diagnostic Sensor}} \cdot \underbrace{\left( \frac{\partial \theta_z}{\partial H} \right)}_{\text{2. Plant Impedance}} \cdot \underbrace{\left( \frac{\partial H}{\partial K_h} \right)}_{\text{3. Flux Actuator}} \cdot \underbrace{\left( \frac{\partial K_h}{\partial Ri_g} \right)}_{\text{4. Closure Slope}} \tag{1}\]

Each link in the chain represents a specific physical or diagnostic component:

1. **Diagnostic Sensor Gain (\(\frac{\partial Ri_g}{\partial \theta_z}\)):** Measures how changes in atmospheric lapse rate (\(\theta_z \equiv \frac{\partial \theta_v}{\partial z}\)) update the local gradient Richardson metric.
2. **Plant Transfer Impedance (\(\frac{\partial \theta_z}{\partial H}\)):** Relates heat flux divergence across the lowest model level to changes in the near-surface potential temperature gradient.
3. **Flux Actuator Sensitivity (\(\frac{\partial H}{\partial K_h}\)):** Converts changes in turbulent eddy diffusivity (\(K_h\)) into changes in downward sensible heat flux (\(H_0 = -\rho c_p K_h \theta_z\)).
4. **Closure Actuator Slope (\(\frac{\partial K_h}{\partial Ri_g}\)):** The derivative slope of the empirical stability function \(\mathcal{K}_h'(Ri_g)\) near critical Richardson thresholds (\(Ri_c \approx 0.20\text{--}0.25\)).

### 2. Fixed-Point Contraction Mapping & BIBO Stability

For a scalar discrete update map \(x_{n+1} = f(x_n)\), a fixed point is locally attracting when its derivative magnitude is below unity:

\[|\mathcal{G}| = \left| \frac{d(Ri_g^{\text{new}})}{d(Ri_g^{\text{old}})} \right| < 1 \tag{2}\]

* **Unstable Regime (\(\lvert\mathcal{G}\rvert > 1\)):** The fixed point becomes repelling. Small numerical fluctuations can amplify across time steps and may produce a spurious surface cold bias.
* **Stable Regime (\(\lvert\mathcal{G}\rvert < 1\)):** The update map is a contraction mapping. Numerical perturbations decay toward equilibrium, subject to the limits of the scalar reduced model.

This is not the BIBO criterion. For a multidimensional discrete SCM, the corresponding local condition is \(\rho(J)<1\), while BIBO stability is a separate input-output property determined by the poles of the relevant transfer function.

### 3. High-Gain Sensor Amplification & Actuator Switching

The pathology of unregularized PBL schemes stems from the interaction between a **singular sensor** and a **high-gain actuator switch**:

* **Sensor Singularity at Low-Level Jet (LLJ) Noses:** The analytical sensor gain evaluates to:
  \[\frac{\partial Ri_g}{\partial \theta_z} = \frac{g}{\theta_0 S^2} \tag{3}\]
  Near the nose of a Low-Level Jet where vertical wind shear vanishes (\(S \equiv \left| \frac{\partial \mathbf{V}}{\partial z} \right| \to 0\)), the diagnostic gain scales as \(\mathcal{O}(S^{-2})\). Differentiating raw, noise-contaminated velocity profiles amplifies observation/grid variance as \(\mathcal{O}(S^{-6})\), injecting massive high-frequency noise power into the top of the gain chain.
* **Actuator Step-Discontinuities:** For closures obtained by inverting a similarity mapping with a finite asymptote \(Ri_c\), the inverse derivative can diverge as \(Ri\to Ri_c^-\). This is closure-specific, not a universal property of MOST; where it occurs, the actuator acts as a high-gain off-switch.

### 4. Closed-Loop Regulation via Upstream C-Z0HR

To bound total loop gain without retuning empirical Monin–Obukhov constants (\(\beta = 5.0\)), **Curvature-Aware Zero-Offset Hyperbolic Regularization (C-Z0HR)** inserts a localized mapping slope \(\mathcal{D}_\alpha^{(R)}(Ri_g)\) into the closure link:

\[\mathcal{G}_{\text{reg}} = \mathcal{G}_{\text{physical}} \cdot \mathcal{D}_\alpha^{(R)}(Ri_g), \qquad \text{where } \mathcal{D}_\alpha^{(R)} \equiv \left. \frac{\partial Ri_g^{\text{reg}}}{\partial Ri_g} \right|_C \tag{4}\]

By evaluating grid-resolved spatial profile curvature

$$C(z, \Delta z) = \frac{1}{2}|Ri_{g,zz}|(\Delta z)^2 + \epsilon_c Ri_c,$$

and writing \(x=Ri_g-Ri_c\) and \(\Phi=\sqrt{x^2+\epsilon_c^2}\), the exact-threshold mapping slope is

$$\mathcal{D}_\alpha^{(R)}(Ri_c) = \frac{\epsilon_c+C}{\epsilon_c+(1+\alpha)C}.$$

Only when \(C\gg\epsilon_c\) does this reduce to:

\[\mathcal{D}_\alpha^{(R)}(Ri_c) \approx \frac{1}{1 + \alpha} \tag{5}\]

For standard damping control **\(\alpha = 2.0\)**, threshold sensitivity is attenuated by **\(66.7\%\)** (\(\mathcal{D}_2^{(R)} = 1/3\)). This establishes the conditional stability criterion:

\[\left|\mathcal{G}_{\text{physical}}(Ri_c)\right| < 1 + \alpha \implies \left|\mathcal{G}_{\text{reg}}(Ri_c)\right| < 1 \tag{6}\]

Because \(\mathcal{D}_\alpha^{(R)} \to 1.0\) when distance from threshold dominates the regularization scale (\(|Ri_g - Ri_c| \gg C\)), C-Z0HR acts as an **equilibrium-compatible attenuator**. This does not imply \(\mathcal{D}_\alpha^{(R)}(Ri_c)\to1\) under grid refinement: \(C\to\epsilon_cRi_c\) as \(\Delta z\to0\), so the exact-threshold value remains controlled by the finite regularization parameters.

### 5. Fast–Slow State-Space Representation (GSPT Integration)

In Geometric Surface Pattern Theory (GSPT), the control-theoretic SCM column is mapped onto a **singularly perturbed fast–slow state space** (\(\epsilon \ll 1\)):

* **Fast Variable:** Microscale Turbulent Kinetic Energy (\(e\), relaxation timescale \(\epsilon \ll 1\)):
  \[\epsilon \frac{de}{dt} = \mathcal{P}(e, S) - \mathcal{B}(e) - \varepsilon_{\text{diss}}(e) \tag{7}\]
* **Slow Variable:** Mesoscale Wind Shear (\(S\), geostrophic forcing timescale):
  \[\frac{dS}{dt} = G_0 - \gamma_s e S - r_s S \tag{8}\]

The fast nullcline \(F(e, S) = 0\) defines the critical manifold \(\mathcal{C}_0\) where turbulence "rides" slow geostrophic forcing. Transverse stability along \(\mathcal{C}_0\) is governed by the fast eigenvalue:

\[\lambda_f(e, S) = \frac{1}{\epsilon} \frac{\partial F}{\partial e} \tag{9}\]

The manifold is normally hyperbolic and attracting wherever \(\lambda_f < 0\). The condition \(\lambda_f=0\) marks loss of normal hyperbolicity; it does not by itself establish a saddle-node. A scalar saddle-node additionally requires, for example, \(F=0\), \(F_e=0\), \(F_{ee}\ne0\), and a suitable unfolding/transversality condition.

**The Decoupling Principle:** GSPT proves that spatial coordinate profile turning points (\(\zeta_z = 0\), "Fold Illusions" caused by LLJ coordinate compression) are logically independent of fast eigenvalue zero-crossings (\(\zeta_z = 0\; \not\!\!\!\!\!\iff \lambda_f = 0\)). Gating relaminarization triggers in NWP closures by \(\lambda_f \ge 0\) prevents SCMs from treating pure coordinate-stretching geometry as physical turbulence collapse, eliminating false bifurcation triggers and solving surface cold-bias divergence.

### 5. Vertical Curvature Kinematics: $Ri_{zz}$ for $Ri = \mathcal{R}(\zeta(z))$

Expanding the gradient Richardson number as a composite function of the Monin–Obukhov similarity variable $\zeta(z) \equiv z / L(z)$, where $L$ is the local Obukhov length:

$$Ri(z) = \mathcal{R}(\zeta(z))$$

Applying the chain rule yields the first and second vertical spatial derivatives:

$$\frac{\partial Ri}{\partial z} = \mathcal{R}'(\zeta) \zeta_z$$

$$Ri_{zz} \equiv \frac{\partial^2 Ri}{\partial z^2} = \mathcal{R}''(\zeta) (\zeta_z)^2 + \mathcal{R}'(\zeta) \zeta_{zz} \tag{7}$$

In the constant-flux surface layer where vertical gradients in Obukhov length are negligible ($\zeta_z = 1/L$ and $\zeta_{zz} = 0$), equation (7) simplifies to the continuous spatial curvature mapping:

$$Ri_{zz} = \frac{1}{L^2} \mathcal{R}''(\zeta) \tag{8}$$

### 6. Analytical Behavior of $\mathcal{R}''(\zeta)$ across Turbulence Regimes

The curvature spectrum $Ri_{zz}$ dictates how analytical stability functions map onto discrete computational grids:

* **Classic MOST Monotonicity:** For standard stability functions $\phi_m(\zeta) = \phi_h(\zeta) = 1 + \beta \zeta$ (with $\beta \approx 5.0$), the function reduces to $\mathcal{R}(\zeta) = \frac{\zeta}{1 + \beta \zeta}$. The second derivative is strictly negative across all stable states ($\zeta > 0$):

$$\mathcal{R}''(\zeta) = -\frac{2\beta}{(1 + \beta \zeta)^3} \implies Ri_{zz} = -\frac{2\beta}{L^2 (1 + \beta \zeta)^3} \tag{9}$$

As $\zeta \to \infty$, physical curvature vanishes ($Ri_{zz} \to 0$), reflecting smooth asymptotic decay of turbulent shear capacity under strongly stable stratification.

* **Non-Linear Functions & Critical Inflection:** An inflection point satisfies $\mathcal{R}''(\zeta_{\text{inf}})=0$ and does not by itself imply a derivative spike. Large curvature occurs when either $|\mathcal{R}''\zeta_z^2|$ or $|\mathcal{R}'\zeta_{zz}|$ is large.

### 7. Coupling $Ri_{zz}$ to Grid-Scale C-Z0HR Attenuation

Substituting the dimensionless grid resolution parameter $\hat{\Delta z} \equiv \frac{\Delta z}{L}$ into the profile curvature metric $C(z, \Delta z)$ connects continuous physical scaling directly to discrete numerical regularization:

For variable \(L\), define \(\hat\zeta_z=L\zeta_z\) and \(\hat\zeta_{zz}=L^2\zeta_{zz}\). Then

$$Ri_{zz}\Delta z^2 = \left[\mathcal{R}''(\zeta)\hat\zeta_z^2 + \mathcal{R}'(\zeta)\hat\zeta_{zz}\right]\hat{\Delta z}^{\,2}, \qquad \hat{\Delta z}=\frac{\Delta z}{L}.$$

Therefore the dimensionless curvature metric is

$$C(\zeta, \hat{\Delta z}) = \frac{1}{2}\left|\left[\mathcal{R}''(\zeta)\hat\zeta_z^2 + \mathcal{R}'(\zeta)\hat\zeta_{zz}\right]\hat{\Delta z}^{\,2}\right| + \epsilon_c Ri_c. \tag{10}$$

Under spatial discretization, $C(\zeta, \hat{\Delta z})$ acts as an adaptive filter:

1. **Under-Resolved Grids / High Physical Curvature ($\hat{\Delta z} \gg 1$ or $|Ri_{zz}| \gg 0$):** The spatial metric can expand, reducing the local mapping slope toward $1/(1+\alpha)$ when $C\gg\epsilon_c$. This supports a local scalar contraction only when the complete scalar derivative satisfies the corresponding bound; it does not guarantee stability of a full SCM.
2. **Asymptotic Convergence Limit ($\hat{\Delta z} \to 0$):** Grid-resolved curvature vanishes and $C\to\epsilon_c Ri_c$. Away from exact threshold, $|Ri-Ri_c|\gg C$ recovers the unregularized map; at exact threshold, the finite value of $\mathcal{D}_\alpha^{(R)}(Ri_c)$ remains.
